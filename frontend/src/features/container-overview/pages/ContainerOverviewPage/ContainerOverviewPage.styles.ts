import { SxProps, Theme } from '@mui/material/styles';

export const containerOverviewPageStyles = {
  root: {
    minHeight: '100vh',
    backgroundColor: '#F7F9FD',
    padding: 0,
  } as SxProps<Theme>,
};
