import React from 'react';

import { MoreVert } from '@mui/icons-material';
import { SvgIconProps } from '@mui/material';

const MoreVertIcon: React.FC<SvgIconProps> = (props) => {
  return <MoreVert {...props} />;
};

export default MoreVertIcon;
